version: '3.8'

services:
  message-manager:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    container_name: Message-manager-v2    restart: unless-stopped
    environment:
      - NODE_ENV=production
    networks:
      - message-network

  # Optional: Include Message API for complete stack
  # Uncomment the section below if you want to run Message API alongside the manager

  # message-api:
  #   image: messageapi/message-api:latest
  #   ports:
  #     - "8080:8080"
  #   container_name: message-api
  #   restart: unless-stopped
  #   environment:
  #     - DATABASE_PROVIDER=postgresql
  #     - DATABASE_CONNECTION_URI=postgresql://postgres:password@postgres:5432/message
  #     - AUTHENTICATION_API_KEY=your-api-key-here
  #     - SERVER_PORT=8080
  #   depends_on:
  #     - postgres
  #     - redis
  #   networks:
  #     - message-network

  # postgres:
  #   image: postgres:15-alpine
  #   container_name: message_postgres
  #   restart: unless-stopped
  #   environment:
  #     - POSTGRES_DB=message
  #     - POSTGRES_USER=postgres
  #     - POSTGRES_PASSWORD=password
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"
  #   networks:
  #     - message-network

  # redis:
  #   image: redis:7-alpine
  #   container_name: message-redis
  #   restart: unless-stopped
  #   ports:
  #     - "6379:6379"
  #   networks:
  #     - message-network

networks:
  message-network:
    driver: bridge

# volumes:
#   postgres_data:
